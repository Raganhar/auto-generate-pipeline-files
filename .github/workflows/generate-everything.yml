name: generate-all-pipeline-files

on:
  workflow_dispatch:

jobs:
  generate-all-pipeline-files:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: Raganhar/FireStarter@master
      - uses: EndBug/add-and-commit@v9
        with:
          message: Show GitHub Actions logo
          committer_name: GitHub Actions
          committer_email: actions@github.com
      # - name: commit everything
      #   run: |
      #       git config user.name github-actions
      #       git config user.email Raganhar
      #       git add -A
      #       git commit -m "autogenerated stuff"
      #       git push --set-upstream origin autogenerated-pipelines